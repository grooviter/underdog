=== Pie
:sources: ../../../../src/test/groovy/underdog/guide/plots

According to Wikipedia:

"A pie chart (or a circle chart) is a circular statistical graphic which is divided into slices to illustrate numerical proportion."
-- Wikipedia, https://en.wikipedia.org/wiki/Pie_chart

In a pie chart, the arc length of each slice (and consequently its central angle and area) is proportional to the quantity it represents.

==== Simple

[source, groovy]
.building pie
----
include::{sources}/PieSpec.groovy[tag=simple, indent=0]
----

<1> slice labels
<2> slice values

image::plots/pie_simple.png[align=center, width="50%"]

==== Color mapping

[source, groovy]
.color mapping
----
include::{sources}/PieSpec.groovy[tag=color_mapping, indent=0]
----

<1> Colors matching the labels
<2> Labels
<3> Values
<4> Passing color mappings

image::plots/pie_color_mapping.png[align=center, width="60%"]

==== Dataframe

[source, groovy]
.dataframe
----
include::{sources}/PieSpec.groovy[tag=dataframe, indent=0]
----

image::plots/pie_dataframe.png[align=center, width="50%"]

[source, groovy]
.series
----
include::{sources}/PieSpec.groovy[tag=series, indent=0]
----

<1> using series **A** and renaming it to **names**
<2> using series **B** and renaming it to **values**
<3> using series **C** and renaming it to **colors**

image::plots/pie_series.png[align=center, width="50%"]