name: pull-requests
on:
  - pull_request

jobs:
  ci:
    runs-on: docker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version: '17'
      - name: build and test
        run: ./gradlew build